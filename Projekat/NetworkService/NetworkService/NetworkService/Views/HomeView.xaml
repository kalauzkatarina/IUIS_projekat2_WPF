<UserControl
    x:Class="NetworkService.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NetworkService.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="700"
    d:DesignWidth="450"
    mc:Ignorable="d">
    <Grid>
        <TextBlock
            Margin="0,20,0,10"
            HorizontalAlignment="Center"
            FontSize="20"
            FontWeight="Bold"
            Text="{Binding Title}" />

        <StackPanel
            Width="250"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Button
                Height="50"
                Margin="0,0,0,30"
                Command="{Binding NavigateNetworkEntitesCommand}"
                Content="Network Entities" />
            <Button
                Height="50"
                Margin="0,0,0,30"
                Command="{Binding NavigateNetworkDisplayCommand}"
                Content="Network Display" />
            <Button
                Height="50"
                Command="{Binding NavigateMeasurementGraphCommand}"
                Content="Measurement Graph" />
        </StackPanel>

    </Grid>
</UserControl>
