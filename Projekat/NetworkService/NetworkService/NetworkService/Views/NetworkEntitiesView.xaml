<UserControl
    x:Class="NetworkService.Views.NetworkEntitiesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NetworkService.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="700"
    d:DesignWidth="450"
    mc:Ignorable="d">
    <Grid Background="White">
        <TextBlock
            Margin="0,20,0,10"
            HorizontalAlignment="Center"
            FontSize="20"
            FontWeight="Bold"
            Text="{Binding Title}" />

        <StackPanel
            Grid.Row="1"
            Margin="0,0,0,10"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Orientation="Horizontal">

            <ComboBox
                Width="60"
                Margin="10,0,10,0"
                ItemsSource="{Binding TrafficTypesList}"
                SelectedItem="{Binding SelectedTrafficType}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image
                                Width="20"
                                Height="20"
                                Margin="0,0,5,0"
                                Source="{Binding TypeIconPath}" />
                            <TextBlock Text="{Binding TrafficTypes}" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                <RadioButton
                    Margin="0,0,10,0"
                    Content="&lt;"
                    GroupName="ValueFilter"
                    IsChecked="{Binding IsLessChecked}" />
                <RadioButton
                    Margin="0,0,10,0"
                    Content="&gt;"
                    GroupName="ValueFilter"
                    IsChecked="{Binding IsGreaterChecked}" />
                <RadioButton
                    Content="="
                    GroupName="ValueFilter"
                    IsChecked="{Binding IsEqualChecked}" />
            </StackPanel>

            <TextBox
                Name="ValueTextBox"
                Width="80"
                Margin="5,0,10,0"
                Text="{Binding SearchValue, UpdateSourceTrigger=PropertyChanged}" />

            <Button
                Width="50"
                Command="{Binding SearchCommand}"
                Content="Search" />

            <Button
                Width="50"
                Command="{Binding ResetCommand}"
                Content="Reset" />

            <Button
                Width="50"
                Command="{Binding DeleteCommand}"
                Content="Delete" />
        </StackPanel>


        <Border
            Grid.Row="2"
            Width="400"
            Height="250"
            Padding="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Background="White"
            BorderBrush="Gray"
            BorderThickness="2"
            CornerRadius="5">
            <DataGrid
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                ItemsSource="{Binding Entities}"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                SelectedItem="{Binding SelectedEntity}"
                SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="50"
                        Binding="{Binding Id}"
                        Header="ID" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Name}"
                        Header="Name" />
                    <DataGridTemplateColumn Width="*" Header="Type">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image
                                        Width="20"
                                        Height="20"
                                        Margin="0,0,5,0"
                                        Source="{Binding TrafficType.TypeIconPath}" />
                                    <TextBlock Text="{Binding TrafficType.TrafficTypes}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding LastValue}"
                        Header="Last Measurement" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
    </Grid>
</UserControl>
